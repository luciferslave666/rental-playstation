// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // (Letakkan semua konfigurasi Next.js kamu di sini)
  // Contoh:
  // reactStrictMode: true,
};

export default nextConfig;